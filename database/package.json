{
  "name": "@bancosatoshi/database",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "ts-node ./src",
    "start:debug": "ts-node-dev --inspect=9226 --no-notify --respawn ./src",
    "clean": "rimraf build",
    "compile": "npm run clean && tsc",
    "publish:prepare": "npm run clean && npm run compile",
    "test": "jest"
  },
  "directories": {
    "build": "build"
  },
  "dependencies": {
    "mysql2": "^2.2.5",
    "sequelize": "^6.5.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "@types/node": "^14.11.8",
    "@types/sequelize": "^4.28.9",
    "jest": "^26.1.0",
    "prettier": "^2.4.1",
    "ts-jest": "^26.1.2",
    "ts-node-dev": "^1.0.0-pre.50",
    "typescript": "^4.0.3"
  }
}